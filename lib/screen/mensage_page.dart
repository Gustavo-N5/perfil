import 'package:flutter/material.dart';

class MensagePage extends StatelessWidget {
  const MensagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Mensage"),
      ),
    );
  }
}
